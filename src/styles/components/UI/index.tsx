import styled from '@emotion/styled';

export const BackBtn = styled.button`
  border: none;
  border-radius: 100px;
  outline: none;
  color: #fff;
  padding: 4px 14px;
  background-color: var(--blue);

  position: fixed;
  top: 8px;
  left: 25px;
  cursor: pointer;
`;
